# Novamedika Project

FastAPI + React application with Traefik reverse proxy.

## Quick Start

### Development
```bash
docker-compose -f docker-compose.traefik.dev.yml up --build


## 🚀 Запуск проекта

### Development:
```bash
# Клонируйте и перейдите в директорию
cd novamedika

# Запустите development окружение
docker-compose -f docker-compose.traefik.dev.yml up --build

# Доступные адреса:
# - Frontend: http://localhost
# - Backend: http://api.localhost
# - Traefik Dashboard: http://localhost:8080
