# Фильтр для аутентификации Backend API
# Обнаружение неудачных попыток входа в систему

[Definition]
failregex = .*Authentication failed.*
            .*Invalid credentials.*
            .*Unauthorized access.*
            .*401 Unauthorized.*
ignoreregex = .*health.*
              .*docs.*
